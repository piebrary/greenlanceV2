

        - make installation script which sets .env files and does npm install etc
        - move mobile and desktop app icons etc to a default folder
        - in mobile app build script make it so it asks for new version number and it updates package.json
        - in documentation specify the scripts file so cli does not have to be used much


        # BACKEND

		- documentatie schrijven
		- testen maken












        # FRONTEND
            - validation addressInput, emailInput, phoneInput
            - errorMessage addressInput, emailInput, phoneInput
            - finish login view (registration/forgot password) loginView
            - email registration, notification etc
            - make all pages
            - check if all code imports are dynamic (try-catch-default-custom)
		- documentatie schrijven
		- testen maken
		- css themes aanpassen
