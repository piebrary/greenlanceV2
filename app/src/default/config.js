module.exports = (() => {

    return {
        AVAILABLE_THEMES:['default', 'green'],
        DEFAULT_THEME:'default',
        AVAILABLE_LANGUAGES:['en', 'nl'],
        DEFAULT_LANGUAGE:'en'
    }

})()
